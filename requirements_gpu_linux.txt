# Plant Disease Detection - Linux GPU Requirements
# For NVIDIA RTX 3050 GPU support on Linux
# Install with: pip install -r requirements_gpu_linux.txt

# Core ML Frameworks - GPU versions
tensorflow[and-cuda]==2.21.0  # GPU-enabled TensorFlow for NVIDIA CUDA
# Optional: use latest stable if needed (2.22+ may require Python 3.13+)
torch>=2.0.0                   # PyTorch with CUDA support
torchvision>=0.15.0            # Computer vision utilities
torchaudio>=2.0.0              # Audio processing

# Ultralytics YOLO - Object Detection/Classification
ultralytics>=8.1.0             # Modern YOLO (automatically uses GPU via PyTorch)

# Web Framework
flask>=2.3.0
werkzeug>=2.3.0

# Image Processing
pillow>=9.0.0
opencv-python>=4.7.0
scikit-image>=0.20.0

# Data Processing
numpy>=1.24.0
scipy>=1.10.0
pandas>=1.5.0
scikit-learn>=1.3.0            # Class-weight balancing used by train.py

# Utilities
tqdm>=4.65.0               # Progress bars
pyyaml>=6.0                # YAML configuration
matplotlib>=3.7.0          # Plotting
seaborn>=0.12.0            # Statistical visualization

# Dataset Tools
kagglehub>=0.1.0           # Kaggle dataset download

# Optional but recommended for GPU optimization
tensorrt>=8.5.0            # TensorFlow optimization
onnx>=1.14.0               # Model format conversion
# onnxruntime-gpu currently has no wheel for Python 3.12 on Linux; install manually if needed:
# pip install onnxruntime-gpu==1.15.0 (if compatible)
# Development & debugging
ipython>=8.10.0
jupyter>=1.0.0
ipywidgets>=8.0.0
black>=23.0.0              # Code formatting
pylint>=2.17.0             # Code linting

# IMPORTANT NOTES FOR LINUX/WSL2 GPU SETUP:
#
# 1. ENSURE NVIDIA DRIVER IS INSTALLED:
#    - Check with: nvidia-smi
#    - Install from: https://www.nvidia.com/en-us/geforce/drivers/
#
# 2. ENSURE CUDA TOOLKIT IS INSTALLED:
#    - TensorFlow 2.15+ requires CUDA 12.3+
#    - Download from: https://developer.nvidia.com/cuda-downloads
#    - Ubuntu WSL2 example:
#      wget https://developer.download.nvidia.com/compute/cuda/repos/wsl-ubuntu/x86_64/cuda-wsl-ubuntu.pin
#      sudo mv cuda-wsl-ubuntu.pin /etc/apt/preferences.d/cuda-repository-pin-wsl-ubuntu
#      wget https://developer.download.nvidia.com/compute/cuda/repos/wsl-ubuntu/x86_64/cuda-repo-wsl-ubuntu-12-4-local_12.4.0-1_amd64.deb
#      sudo dpkg -i cuda-repo-wsl-ubuntu-12-4-local_12.4.0-1_amd64.deb
#      sudo apt-key adv --fetch-keys https://developer.download.nvidia.com/compute/cuda/repos/wsl-ubuntu/x86_64/3bf863cc.pub
#      sudo apt-get update
#      sudo apt-get -y install cuda
#
# 3. ENSURE cuDNN IS INSTALLED:
#    - Download from: https://developer.nvidia.com/cudnn
#    - Should be at /usr/local/cuda/include/cudnn.h
#
# 4. SET ENVIRONMENT VARIABLES:
#    Add to ~/.bashrc or ~/.zshrc:
#    export CUDA_HOME=/usr/local/cuda
#    export PATH=$CUDA_HOME/bin:$PATH
#    export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$LD_LIBRARY_PATH
#
# 5. VERIFY GPU SETUP:
#    python gpu_setup_verification.py
#
# 6. IF USING WSL2:
#    WSL2 already supports GPU passthrough via NVIDIA CUDA on WSL2
#    No special configuration needed - just install CUDA inside WSL2
